<?xml version="1.0" encoding="UTF-8"?>
<Migrations>
  <Migration version="1" id="Create first Table">
    <UpVersion>
        CREATE TABLE USERS (
            ID INT(11) NOT NULL AUTO_INCREMENT,
            NAME VARCHAR(30) NULL DEFAULT NULL,
            EMAIL VARCHAR(30) NULL DEFAULT NULL,
            LOGIN VARCHAR(20) NULL DEFAULT NULL,
            PASSWORD VARCHAR(10) NULL DEFAULT NULL,
            PRIMARY KEY (ID)
        )
        COLLATE='utf8_general_ci'
        ENGINE=InnoDB;
    </UpVersion>
    <DownVersion>
        DROP TABLE USERS;
    </DownVersion>
  </Migration>
  <Migration version="2" id="Insert admin user">
    <UpVersion>
        INSERT INTO USERS
            (NAME, EMAIL, LOGIN; PASSWORD)
        VALUES
            ('ADMIN','admin@admin.com'; 'admin'; 'admin');
    </UpVersion>
    <DownVersion>
        DELETE FROM USERS
        WHERE LOGIN = 'admin'
          AND PASSWORD = 'admin';
    </DownVersion>
  </Migration>
</Migrations>
